# Skill Interface Contract

Each skill spec must define:

- `name`
- `purpose`
- `inputs`
- `steps`
- `requiredChecks`
- `blockingCriteria`
- `outputs` (must follow `output-format.md`)
- `examples`

Skill execution must be deterministic from inputs.
