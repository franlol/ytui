# Plugin Lifecycle Workflow

1. Run `/ytui-plan-change` with plugin scope.
2. Run `/ytui-author-plugin`.
3. Run `/ytui-review-plugin`.
4. Run `/ytui-review-architecture`.
5. Run `/ytui-review-docs-sync`.
6. Attach reports and merge only on `PASS`.
