# Release Lifecycle Workflow

1. Freeze release scope.
2. Run `/ytui-release-audit`.
3. Run `/ytui-review-docs-sync` for release-delta.
4. Resolve all `FAIL`/blocking findings.
5. Update release notes.
6. Tag release.
